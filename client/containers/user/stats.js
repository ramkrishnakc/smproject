import React from 'react';
// import PropTypes from 'prop-types';

const UserStats = () => <div>User statistics is displayed here.</div>;

UserStats.propTypes = {
  // title: PropTypes.string.isRequired,
};
export default UserStats;
