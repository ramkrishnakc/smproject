import React from 'react';
// import PropTypes from 'prop-types';

const Payroll = () => <div>Payroll page</div>;

Payroll.propTypes = {
  // title: PropTypes.string.isRequired,
};
export default Payroll;
