import React from 'react';
// import PropTypes from 'prop-types';

const Admission = () => <div>Admission page</div>;

Admission.propTypes = {
  // title: PropTypes.string.isRequired,
};
export default Admission;
