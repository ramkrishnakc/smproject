import React from 'react';
// import PropTypes from 'prop-types';

const Create = () => <div>add new book page</div>;

Create.propTypes = {
  // title: PropTypes.string.isRequired,
};
export default Create;
