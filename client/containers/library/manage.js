import React from 'react';
// import PropTypes from 'prop-types';

const Manage = () => <div>Available books list page</div>;

Manage.propTypes = {
  // title: PropTypes.string.isRequired,
};
export default Manage;
