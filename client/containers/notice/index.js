import React from 'react';
// import PropTypes from 'prop-types';

const Notice = () => <div>Notice page</div>;

Notice.propTypes = {
  // title: PropTypes.string.isRequired,
};
export default Notice;
